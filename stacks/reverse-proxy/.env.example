# Reverse Proxy Stack (.env.example)
# File: stacks/reverse-proxy/.env.example
#
# Purpose:
# Example environment variables for Traefik reverse proxy stack.
#
# IMPORTANT:
# - Copy this file to .env inside VM200 before deployment.
# - Do NOT commit real secrets into GitHub.

# Required for Let's Encrypt ACME certificates
TRAEFIK_ACME_EMAIL=you@example.com
